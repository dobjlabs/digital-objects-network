# help

The user asked for the command menu. Below these instructions is that menu as a
Markdown table, followed by a final hint line.

## Output rules

- Output the table and the hint line below exactly as written, verbatim -- it is
  already valid Markdown. Do not realign, convert, reword, or add to it.
- No preamble, no closing commentary, and no tool calls -- the menu is complete
  and current.
