# help

The user asked for the command menu. Below these instructions is that menu --
each command as `name -- description`, then a final hint line.

## Output rules

- Plain text only. No markdown headings, bold, bullets, or code fences.
- No preamble, no closing commentary.

## Steps

Output the menu lines below exactly as written, one per line, then stop. Do not
add, drop, reorder, or reword any line, and do not call any tool -- the menu is
already complete and current.
