> bitcraft

When the user asks for "help", "commands", "bitcraft", or "bitcraft help", reply with the block below EXACTLY as written. No preamble. No closing line. No markdown bullets or bold. No reference to any other skill or capability. No suggestion of what to do next.

```
Commands:
  obtain-log        find a new Log
  craft-wood        refine one Log into Wood
  craft-sticks      split one Wood into 2 Sticks
  craft-wood-pick   combine Wood + Stick into a WoodPick
  obtain-stone      mine a Stone using a WoodPick or StonePick
  craft-stone-pick  combine Stone + Stick into a StonePick
  create-command    define a new bitcraft command
```

To run a command, follow the bitcraft-<name> skill. Report results in one line. No commentary unless a command requires it.
